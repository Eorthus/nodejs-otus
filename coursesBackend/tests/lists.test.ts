
describe('lists test', () => {

    test('list exists', async () => {
        // import api courses, users
        // expect(course).toBe(true)
                // expect(users).toBe(true)
    })

    test('courses fields', async () => {
        // import api courses from bd
        // for every item required fields
        // expect(field).toBe(true)
    })

    test('users fields', async () => {
        // import api users from bd
        // for every item required fields
        // expect(field).toBe(true)
    })

    test('courses fields types', async () => {
        // import api courses from bd
        // for every item fields check type
        // expect(typeof field === 'some type').toBe(true)
    })

    test('users fields types', async () => {
        // import api users from bd
        // for every item fields check type
        // expect(typeof field === 'some type').toBe(true)
    })
})