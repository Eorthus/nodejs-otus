
describe('lesson test', () => {

    test('lesson exists', async () => {
        // import api lesson from bd
        // expect(lesson).toBe(true)
    })

    test('lesson fields', async () => {
        // import api lesson from bd
        // for every required fields
        // expect(field).toBe(true)
    })

    test('lesson fields types', async () => {
        // import api lesson from bd
        // for every fields check type
        // expect(typeof field === 'some type').toBe(true)
    })

    test('lesson create', async () => {
        //api create lesson
        //expect(newLesson).toBe(true)
    })

    test('lesson update', async () => {
        //api update lesson
        //expect(LessonField === 'some').toBe(true)
    })

    test('lesson delete', async () => {
        //api delete lesson
        //expect(Lesson).toBe(false)
    })
})